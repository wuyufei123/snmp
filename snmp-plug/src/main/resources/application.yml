 server:
   port: 8080
   servlet:
     context-path: /
 spring:
   datasource:
     username: root
     password: root
     url: jdbc:mysql://localhost:3306/snmp?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
     type: com.alibaba.druid.pool.DruidDataSource
     driver-class-name: com.mysql.cj.jdbc.Driver
   mvc:
     view:
       prefix: /
       #后缀
       suffix: .html
 #mybatis配置
 mybatis:
   mapper-locations: classpath*:/mapper/*.xml